```js
// Напиши что-то внутри функции-конструктора Cat, чтобы при её вызове с new она возвращала объект типа { name: <переданное имя>, isCute: true }
// (в этом конкретном случае cat должен быть равен { name: 'кирилл', isCute: true })

function Cat({ name }) {
  // ?
}

const cat = new Cat({ name: "кирилл" });
```
